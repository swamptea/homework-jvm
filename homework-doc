public class JvmComprehension {

    public static void main(String[] args) {
        int i = 1;                      // 1
        Object o = new Object();        // 2
        Integer ii = 2;                 // 3
        printAll(o, i, ii);             // 4
        System.out.println("finished"); // 7
    }

    private static void printAll(Object o, int i, Integer ii) {
        Integer uselessVar = 700;                   // 5
        System.out.println(o.toString() + i + ii);  // 6
    }
}

Jvm отправляет в подсистему загрузчиков классов классы "JvmComprehension" и "System".
 1: В стеке в фрейме "main" создается int i со значением 1.
 2: В куче в классе Object создается объект класса, затем в стеке в фрейме "main" создается переменная o со ссылкой на него.
 3: В куче в классе Integer создается объект класса, затем в стеке в фрейме "main" создается переменная ii со ссылкой на него.
 4: В стеке создастся новый фрейм printAll и переменные o, i, ii со ссылками на объекты из кучи, с которыми был вызван метод
 5: В куче в классе Integer создается объект класса, затем в стеке в фрейме "printAll" создается переменная uselessVar со ссылкой на него.
 6: В куче создается новый объект типа String, затем в стеке будет создан новый фрейм и переменная со ссылкой на строку из кучи.
 7: В куче создается новый объект типа String, затем в стеке будет создан новый фрейм и переменная со ссылкой на строку из кучи. Объекты из кучи могут быть удалены сборщиком мусора
